OPTIONAL 3D MODE

Place an optimized GLB jewellery model here as:
  public/models/necklace.glb

Then set the corresponding product in src/data/jewellery.ts to active:true and renderMode:"3d".

For production, optimize GLB polygon count, textures and materials before loading on kiosk hardware.
